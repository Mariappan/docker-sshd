apiVersion: apps/v1
kind: Deployment
metadata:
  name: ssh
  namespace: default
spec:
  selector:
    matchLabels:
      app: ssh
  replicas: 1
  template:
    metadata:
      labels:
        app: ssh
    spec:
      containers:
      - name: master
        image: rmariappan/sshd:0.1
        imagePullPolicy: Always
        # command:
        #   - /usr/sbin/sshd
        #   - -D
        #   - -E
        #   - /tmp/ssh.log
        ports:
        - containerPort: 22
      #  readinessProbe:
          # tcpSocket:
          #   port: 22
          # periodSeconds: 10
          # timeoutSeconds: 5
          # successThreshold: 2
          # failureThreshold: 5
        env:
        volumeMounts:
        - name: ssh-dir
          mountPath: /root/.ssh/authorized_keys
          subPath: authorized_keys
        - name: ssh-host-keys
          mountPath: /etc/ssh/hostKeys
        resources:
          limits:
            cpu: 50m
            memory: 30Mi
          requests:
            cpu: 10m
            memory: 20Mi
      volumes:
      - name: ssh-dir
        secret:
          secretName: ssh-keys
          defaultMode: 0600
      - name: ssh-host-keys
        secret:
          secretName: ssh-host-keys
          defaultMode: 0600
